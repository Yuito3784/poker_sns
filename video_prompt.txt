# 動画生成プロンプト（video.py がこの内容から MP4 を組み立てます）
#
# 使い方:
#   1. 下の「台本エリア」に、出したいテロップの文章を書く（改行・段落OK）
#   2. リポジトリのルートで: py video.py
#   3. 出力: hyperframes-marketing-reels/output/from_prompt.mp4
#
# オプション:
#   py video.py --preset marketing
#     → 既存の30秒マーケリール（influencer 含む）をレンダー。プロンプトは output/last_prompt.txt にコピーのみ。
#   py video.py --output path/to/out.mp4
#   py video.py --prompt-file my_prompt.txt
#   py video.py --dry-run   （HTML 生成のみ、レンダーしない）
#
# 前提: Node.js / npx / FFmpeg が入り、hyperframes-marketing-reels で npx hyperframes が動くこと。
# --- 台本エリア（この下を編集）-----------------------------------------

【フック】
まだスクロール、止めないで。

【本題】
2026年4月16日。HeyGen が GitHub に置いた「Hyperframes」。
HTML を書くと、MP4 になる。

Claude Code に話しかけるだけで、
テロップ・カット・尺が組み上がる。

外注で 5〜10 万円かかっていたモーションが、
ほぼゼロに近いコストで回る。

【締め】
いま触った人が、一番早い。
